# 网络层、流式输出与生命周期

Android 端接入大模型时，网络层比普通接口更复杂，因为大模型响应可能持续几秒到几十秒，期间用户可能返回、切后台、断网或取消。

## 普通响应和流式响应

| 响应方式 | 适合场景 |
| --- | --- |
| 普通 HTTP | 短文本、分类、结构化抽取 |
| SSE/流式 HTTP | 聊天、长答案、RAG 解释 |
| WebSocket | 双向事件、多步骤 Agent 进度 |
| WebRTC | 实时语音、低延迟音频会话 |

OpenAI 官方文档说明，Streaming 可以在完整输出生成前先处理模型输出；Realtime 文档建议浏览器/移动端媒体场景优先考虑 WebRTC。

对普通 Android 文本聊天，先用后端转发 SSE 或自定义流式协议就够了。

## Kotlin Repository 骨架

下面是伪代码骨架，真实项目可用 OkHttp、Retrofit 的 Streaming 能力或 Ktor Client：

```kotlin
class AiRepository(
    private val client: OkHttpClient,
    private val baseUrl: String,
    private val tokenProvider: TokenProvider
) {
    fun streamChat(message: String): Flow<ChatEvent> = callbackFlow {
        val request = Request.Builder()
            .url("$baseUrl/ai/chat/stream")
            .header("Authorization", "Bearer ${tokenProvider.token()}")
            .header("Accept", "text/event-stream")
            .post("""{"message": ${jsonEscape(message)}, "stream": true}""".toRequestBody())
            .build()

        val call = client.newCall(request)
        awaitClose { call.cancel() }
    }
}
```

重点不是具体库，而是：

1. 请求自己的后端。
2. 可以取消 call。
3. 把网络事件转换成 `ChatEvent`。
4. 不把网络细节暴露给 UI。

## 生命周期取消

ViewModel 里发起请求时，应该绑定 `viewModelScope`。页面离开或任务取消时，取消对应 Job。

```kotlin
private var currentJob: Job? = null

fun send(message: String) {
    currentJob?.cancel()
    currentJob = viewModelScope.launch {
        repository.streamChat(message).collect { event ->
            reduce(event)
        }
    }
}

fun cancel() {
    currentJob?.cancel()
    reduce(ChatEvent.UserCancelled)
}
```

Android 官方文档也强调 ViewModel 是屏幕级状态持有者，能在配置变化时保留状态，适合承载这类业务状态。

## 迟到事件

取消后仍可能收到网络层迟到事件。状态机要忽略迟到 delta：

```kotlin
if (state.status == ChatStatus.Cancelled) {
    return
}
```

这能避免用户点了取消，界面又继续追加文字。

## 弱网处理

弱网下要明确区分：

1. 超时：提示重试。
2. 断网：提示检查网络。
3. 429：提示稍后再试。
4. 401：重新登录。
5. 5xx：服务暂时不可用。
6. 用户取消：不要显示错误。

Android 端不要把后端原始错误直接展示给用户，而要映射成稳定用户文案。

## 实战例子：弱网、取消和重试

下面是一组贯穿项目里很常见的用户路径：

| 场景 | Repository 行为 | ViewModel 行为 | UI 行为 |
| --- | --- | --- | --- |
| 首 token 前超时 | 抛出 `TimeoutBeforeFirstDelta` | assistant 消息进入 `Error` | 显示“连接超时，重新生成” |
| 已流式输出一半后断网 | 抛出 `StreamInterrupted(partial=true)` | 保留已生成内容并标记失败 | 显示“已中断，可重新生成” |
| 用户点击取消 | `call.cancel()`，停止收集 Flow | 状态进入 `Cancelled` | 立即停止动画，不显示错误 |
| 取消后迟到 delta | 事件被 Repository 或 reducer 丢弃 | 不改变消息内容 | 用户看不到取消后追加 |
| 429 限流 | 解析 `retry_after_seconds` | 标记 `retryable=false` 或延迟可重试 | 显示倒计时 |

一个简单的错误映射边界可以是：

```kotlin
sealed interface AiFailure {
    data object NetworkUnavailable : AiFailure
    data object TimeoutBeforeFirstDelta : AiFailure
    data class StreamInterrupted(val partial: Boolean) : AiFailure
    data class RateLimited(val retryAfterSeconds: Int?) : AiFailure
    data object Unauthorized : AiFailure
    data object UserCancelled : AiFailure
    data class ServerError(val traceId: String?) : AiFailure
}
```

Repository 负责把 HTTP、SSE、OkHttp 异常和后端错误码转换成 `AiFailure`。ViewModel 只关心业务含义，不直接判断供应商错误文本。
